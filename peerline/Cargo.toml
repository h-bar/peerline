[package]
name = "peerline"
description = "Peer-symmetric JSON framing toolkit — bidirectional request/response/notification/stream envelopes with a pubsub layer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
