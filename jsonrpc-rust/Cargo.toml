[package]
name = "jsonrpc-rust"
description = "Transport-agnostic JSON-RPC 2.0 toolkit with the pubsub subscription extension"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
