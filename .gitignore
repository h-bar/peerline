/target
# Belt and braces: each JS package also ignores its own, but a package
# directory being removed must not un-ignore what is left behind.
node_modules/
**/*.rs.bk
.DS_Store

# Playwright MCP scratch output
.playwright-mcp/
